#!/usr/bin/env python
try:
    import metgem
except ImportError:
    import sys
    import os
    sys.path.insert(0, os.path.realpath(os.path.join(os.path.dirname(__file__), '..')))
    import metgem

metgem.run()
